#pragma once

#include "../resource.h"

// Function declarations for tray icon management
void AddTrayIcon(HWND hWnd);    // Declare the AddTrayIcon function
void RemoveTrayIcon();          // Declare the RemoveTrayIcon function
void CreateTrayMenu();          // Declare the CreateTrayMenu function